import { COLORS } from '@/constants/styles/colors';
import styled from '@emotion/native';

const StorybookWrap = styled.View({
  backgroundColor: COLORS.grayscale.white,
  flex: 1,
  justifyContent: 'center',
});

export default StorybookWrap;
