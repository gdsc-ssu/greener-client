import StyledText from '@/components/common/atoms/StyledText';
import styled from '@emotion/native';

export const AddressWrap = styled.View({
  flexDirection: 'row',
  marginTop: 4,
  alignItems: 'center',
});

export const AddressText = styled(StyledText)({
  marginLeft: 4,
});
